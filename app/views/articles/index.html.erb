<h1><%= @board.title %></h1>
<h3>内容：<%= @board.intro %></h3>

<%= link_to '記事作成', new_board_article_path %>

<hr>

<% @board.articles.each do |article|%>
<p><%= article.user.name %></p>
<p><%= article.content %></p>
<p><%= article.created_at %></p>
<%= link_to 'Edit', edit_board_article_path(@board,article) %>
<%= link_to 'Destroy', board_article_path(@board,article), method: :delete, data: { confirm: 'Are you sure?' } %>
<hr>
<% end %>

<%= link_to '掲示板一覧へ', boards_path  %>

